name: Pull Request title

on:
  pull_request:
    types:
      - opened
      - edited
      - synchronize

permissions:
  pull-requests: read  # required by action-semantic-pull-request (via the reusable pull-request workflow) to read the PR title

jobs:
  pull-request:
    uses: ./.github/workflows/pull-request.yaml
